<div class="well" id="<%= post.id %>">
  <h4><%= link_to post.title, post_path(post) %></h4>
  <p><%= truncate(post.description, length: 100) %></p>

  <div style="border-top: 1px solid #ddd; padding-top: 10px">
  <%= render 'settings', post: post, useAjax: true %>
</div>
</div>
